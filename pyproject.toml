[project]
name = "ghost-chain"
version = "0.1.0"
description = "Ghost Chains Phase 1 — real-time AML transaction risk scoring"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask==2.2.5",
    "gunicorn==21.2.0",
]
