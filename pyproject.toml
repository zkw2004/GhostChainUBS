[project]
name = "ghost-chain"
version = "0.1.0"
description = "Ghost Chains Phase 1 — real-time AML transaction risk scoring"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn==0.32.1",
    "pydantic==2.10.4",
]
